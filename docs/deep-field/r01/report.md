# DEEP FIELD — Round 1

## Base decision

1. `index.ts` stays: one canvas, one renderer, scroll as sole progress, `released()` fade, `__mmNativeMatchMedia`, inert, focus rescue, seek.
2. `chapters.ts` keeps its pure `evaluate()`; twelve beats, derived from `featured.ts`.
3. Retired: `camera.ts`, `shapes.ts`, `artifacts.ts`, `particles.ts` — object-stage only.
4. New `field.ts`: ONE population (80k / 25k), sky and morph material. No second engine.
5. New `targets.ts`: figures from the real name and real key images; sampling split from seating, so resizing is a multiply.
6. `Chapter/Stage.astro` keep the runway, seek nav, skip link; now heading, line, links.
7. `signal.css` keeps its structure and both seams; re-toned `#000` / `#F0F3F6` / `#8AB4FF`.
8. `test-signal.py` keeps its shape; plate and handoff checks retired, reasons inside.
9. One progress bar: the site's own, re-toned — the isolated stage cannot reach above the header.
10. The camera never moves; the dolly is a field term, which keeps seats camera-relative.

## Shipped

Wrapped tunnel, drift, 0.3° parallax, adaptive count, grain, vignette, haze, chrome, hairline, reduced motion. Constellation morph, stable point identity: the hero name, plus `world-ask-repos` edge-sampled from its key image. Ten chapters carry the field alone. Canvas fades before the archive. EN and AR ship. One finish-review pass closed nine items, including a second type family in the stage and a wordmark spelling the name above the hero.

## Budgets

`node scripts/serve-static.mjs dist 4618`; `measure-deep-field.py`; `test-signal.py`.

| Bar | Measured |
|---|---|
| desktop frame ≤18.2 ms | **6.1** p95 |
| phone-emu ≤33.3 ms | **6.1** p95 |
| draw cost | **0.057 / 0.018 ms** |
| JS ≤350 KB gz | **201.4** |
| paint <1.5 s | **0.23 s** localhost |
| CLS 0 | **0.00031 / 0** |
| contrast ≥4.5:1 | **18.9 / 9.6 / 9.9** |
| h-scroll at 390 | **0 px** |
| suite | **398/398** |

Controls: draw cost scales with the star count, R² 0.998. A planted clock term and a 2.55:1 colour reddened 84 checks.

## Red

- **No frame rate is claimed.** Intervals fail to quantise to the 240 Hz period, so the loop is not display-locked and the interval only bounds frame time. Budgets are milliseconds.
- Phone figures are emulation on an RTX 5070 Ti. Round 3 owns a device.
- CLS is 0 at 390 and **0.00031** at 1440: the shared header's webfont swap, not the stage. Fixing it is a site-wide font change.
- No career-reel video in this repo. Film ships as the nine published film pages.

Draft: https://6aad6814b3d3a8a12e05e050--mohamed-mahmoud-portfolio.netlify.app/en/

## Next

Round 2: the seven remaining Worlds morphs, the Film reel plane (source is a decision), the Public constellation, the Contact star.

# Round 5 implementation and evidence boundaries

Executor: Astra. Historical Opus/Fable role names in the brief are context; the current owner authorized this execution directly.

## Test changes

Round 4's geometry, reading, accessibility, archive, language, determinism and fallback checks remain. Nineteen original beats expanded to forty-six. Twenty-one stations intentionally have no reading hold; the old assertion that every figure stops was replaced by a no-stop assertion for stations and the original stop assertion for reading/gate beats. The sixteen Workshop stations replace the incorrect twelve installed-tool labels. Navigation visits them as one Workshop stop. Six silent clips now include the approved Daheeh loop. The removed eyebrow is no longer a contrast selector. The world hold floors remain 700/450/400 pixels for assembly/hold/release.

New checks use the actual uploaded star homes, actual DOM plate box, network requests, decoded video time, and applied hologram frame. Flat-depth geometry, a two-frame plate lag, an early media request, an altered clip time, a non-zero hold bend, and forbidden raw game markup are negative controls. `suite.json` records every assertion. `registration.json` retains the real-wheel samples.

## Media

Five world clips are silent 960×540 H.264, five seconds each, paired with the existing keyframes. `media-provenance.json` contains exact source and output hashes. El Daheeh uses the owner's `char1.mp4`, explicitly approved for public portfolio use in this task. Its first 2.5 seconds followed by their reverse make a silent five-second loop; the first frame makes the 800×800 AVIF and Sobel edge figure.

`voice-provenance.json` identifies the real held-out external fixture and clone hashes. Its shapes demonstrate calibration, not owner-likeness acceptance. No audio or private score is shipped. Hazalqoum, Daheeh and Bayoumi have recovered clone envelopes. Keeber and Lemby use plain rings because their clone outputs were not recovered. The other four illustrations and loops were absent at both intake checks. No replacement likeness was generated.

Game films are 96 frames × 4,000 points, at 12 fps for eight seconds. The pipeline normalizes Sobel magnitude to each frame's 97th percentile and uses a deterministic, jittered spatial grid with weighted sampling: a stratified approximation to blue-noise sampling. Each `.darb` file contains gzip-compressed Uint16 x/y pairs and a twelve-byte DARB header. `.gz` responses unexpectedly arrived empty in Chrome on this host while command-line reads succeeded; the same bytes served with `.darb` loaded correctly. The cause was not established. Loading errors are retained by the probe without repeated per-frame requests.

Polyblast and ARTILLERY3D are fresh Godot movie captures. WARSTRIKE's packaged launch did not produce a capturable window. Cocolani's attempted movie was rejected for missing imported assets and an unpopulated scene. Their permitted fallback uses their own boot screenshots. Sources, hashes and the four false `raw_allowed` flags are in `sources.json`. Raw review files remain local under `work/game-review`; the review sheet exports decisions, and no decision is implied by opening it.

## Performance and delivery

The deferral comparison uses the same completed scene, three Lighthouse runs per preset: static import versus import after the native page's first paint opportunity. The final visual pass raised voice-contour density, brightened gold rims, restored 3D anchor hairlines, and dimmed the background during voice beats. These refinements followed the controlled deferral experiment; the final build has fresh frame, paint and JavaScript-budget measurements. Cold simulated mobile loading remains slow; passing local paint and animation budgets does not establish phone acceptance. The owner’s physical phone remains the acceptance surface.

Only the v2 Pages repository is a deployment target. The first site's `main`, `gh-pages`, public junction and world assets are preserved. World links remain absolute links to the existing published films.

Visual inspection caught weak voice contrast after the structural suite passed. The local before image is retained in work/voice-before-visibility.jpg; the final voice sheet shows the repaired result. Full-screen pass-through duration is measured at all four viewport corners, above ten-percent plate opacity, in timing.json.

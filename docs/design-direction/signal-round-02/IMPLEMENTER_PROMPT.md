# Copy-ready implementer prompt

Continue the existing **From Signal to Systems** implementation using the director's Round 02 visual pack. Do not restart concept selection or reimplement the framework.

Repository: `Mohamed3042/flagship-portfolio`
Director branch: `design/signal-director-pack-20260918`
Director folder: `docs/design-direction/signal-round-02/`

Read the latest discussion on the director-pack draft PR, then read `DIRECTOR_HANDOFF.md`, `REVIEW_PROTOCOL.md` and `ASSET_MANIFEST.json`. Open the compact preview and the four full-resolution boards when the download pack is available. Use the images for scale, light, material and spatial relationships. Do not copy their fabricated product names, dashboard figures, nav labels, human-review portrait or unrelated space World.

Inspect the CURRENT local Signal worktree, source, review notes and git status before making edits. The previous inspection found `feature/signal-to-systems` based on `9e89e31` with uncommitted code; the committed SHA alone does not identify that work. Preserve all existing edits and shared junction targets. Do not reset, stash, clean, switch the active checkout, install into shared node_modules, or overwrite shared public assets.

Preserve the working native-scroll evaluator, point identity, reverse reconstruction, HTML content, bilingual routes, existing Worlds and source-of-truth project data. Make the next milestone one complete sequence on desktop AND portrait: a clear immense horizon with useful actions; an aligned star form; an unmistakable depth reveal; and a stable handoff into a readable real automation screen.

Then fix the material and portal chapters: let the particle cage recede behind a tangible carton; connect a camera-aware interior to the actual selected World rather than floating a Cake Studio screenshot over unrelated terrain; and land the sequence visibly in real project entries. Restore full limitation access and repair route-scoped theme/contrast issues at their actual owning selectors.

The persistent datum organizes reading stops, not every frame. Cropping is allowed for atmospheric scale, not for hiding the phone's subject or important screenshot content. Recompose around protected text/action regions instead of making another isolated rim-offset nudge.

The old `.impeccable/review/finish-review.md` describes the showroom. The newer `docs/signal-review/README.md` describes Signal and records open issues. Do not borrow an old verdict or report untested results. Use fresh commit/diff identity and capture provenance.

Submit in the SAME draft PR conversation as `IMPLEMENTER / Round 02`, using the reply template. Include the implementation SHA or scoped dirty-diff hash, changed files, eight desktop/portrait key poses, a normal-paced forward/reverse recording through the real project rows, Arabic/reduced-motion checks, test logs, and explicit untested/device limits. Mark criteria addressed/partial/blocked/not tested. Keep the first scope focused; do not bury visual regressions under a bigger test count.

Continue autonomously through the directed code and evidence pass. Stop only for a real rights/scope/destructive-operation blocker. No production deployment, paid asset run, unrelated cleanup or automatic merge. The owner approves release; the director reviews the submitted visual evidence.

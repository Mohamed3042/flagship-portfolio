# Academy WAN returns

Drop the 16 original WAN 2.7 downloads here with these exact names:

`ACA-001.mp4` through `ACA-016.mp4`

Do not trim, rename, recompress, or remove any burned watermark before the acceptance gate. “Done” on the generation board means generated/downloaded only; it does not mean accepted.

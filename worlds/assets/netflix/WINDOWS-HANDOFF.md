# Netflix World — Windows continuation handoff

## Current checkpoint

- Branch: `main`
- Phase 1: **complete and owner-approved**
- Approved stills: **33 / 33** at exact **1920×1088 PNG**
- Hostile-editor result: **33 PASS / 0 REJECT**
- WAN clips: **0 / 32 generated by Codex**
- WAN credits spent by Codex: **0**
- Next owner action: generate the 32 WAN 2.7 clips from the copy-ready runbook using Mohamed's
  own account
- Page implementation/deploy: intentionally waiting for accepted MP4s

## Start on Windows

```powershell
git clone https://github.com/Mohamed3042/flagship-portfolio.git
cd flagship-portfolio
git switch main
git pull --ff-only origin main
cd public\worlds\assets\netflix\wan
```

Open [`wan/OWNER-RUNBOOK.md`](./wan/OWNER-RUNBOOK.md). It contains the locked settings, credit
cap, shared negative prompt, 32 positive prompts, exact reference/FLF map, seeds, accepted output
filenames, retake ladder, and resumable task log.

For each accepted clip:

1. Save the immediate download to `wan\accepted\NFX-A-###.mp4`.
2. Run `./extract-endframe.ps1 -ClipId NFX-A-###` when native continuation is unavailable.
3. Use that generated end frame as the next clip's first-frame input.
4. Update the task row in `wan\OWNER-RUNBOOK.md` with provider task ID, applied seed, status,
   credits, saved file, and notes.
5. Push checkpoints regularly so the generation chain is recoverable.

## Resume prompt for Codex on Windows

Paste this after all accepted clips are pushed:

> Continue Netflix World — THE ANTHOLOGY from the synced checkpoint. Read `CLAUDE.md`,
> `public/worlds/assets/netflix/WINDOWS-HANDOFF.md`,
> `public/worlds/assets/netflix/keyframe-review.md`, and
> `public/worlds/assets/netflix/wan/OWNER-RUNBOOK.md` first. Verify exactly 32 accepted WAN MP4s
> and reconcile the run log/credits. Then perform common watermark-band detection and identical
> crop if required, normalize the clips, build the scroll-scrubbed `public/worlds/netflix.html`
> by extending `cinema.js`/`cinema.css`, implement the six free code scenes and mobile/reduced-
> motion contracts, run three QA passes plus build/live verification, update the visible manifest
> version badge and changelog, commit, push, and deploy exactly as the original production prompt
> authorizes. Do not add GitHub Actions.

Do not regenerate the approved keyframes unless Mohamed explicitly requests a new revision.
